SELECT name, salary, salary + 5000 AS increased_salary
FROM employees;


SELECT name, salary, salary - 1000 AS reduced_salary
FROM employees;


SELECT name, salary, salary * 1.10 AS bonus_salary
FROM employees;



SELECT name, salary, salary / 12 AS monthly_salary
FROM employees;
